<% provide(:title, 'Blog') %>
<body>
  <%= render  "shared/header" %>
  <h1>All users</h1>
  <%= will_paginate(@users, :renderer => WillPaginate::ActionView::Bootstrap4LinkRenderer) %>
  <ul class="users">
    <% @users.each do |user| %>
      <%= render user %>
    <% end %>
  </ul>
</body>
